// redux/reducer/products.js
const initialState = [
    // Tus productos aquí
  ];
  
  const products = (state = initialState, action) => {
    switch (action.type) {
      default:
        return state;
    }
  };
  
  export default products;
  